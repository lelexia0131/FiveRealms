# FiveRealms Skill

## 一、任务目标

本规范主要用于 FiveRealms 的单 Bug 修复任务。

如果用户明确要求进行平衡调整、文档修改、代码审计、UI 优化或其他非 Bug 任务，
应以用户本次明确目标为准，但仍须遵守本文中的工作目录、Git、最小范围、
测试、构建版本和完成报告要求。

未经用户明确要求，不得把 Bug 修复扩大为功能开发、平衡调整或无关优化。

## 二、工作目录与权限

主要工作目录：

```
D:/FiveRealms
```

允许在该目录内：

- 读取和搜索项目文件
- 查看 Git 状态、历史和 diff
- 修改与当前 Bug 直接相关的代码
- 修改或新增与当前 Bug 直接相关的测试
- 可以在 `D:/FiveRealms` 内创建当前 Bug 验证所必需的临时文件；完成验证后必须删除，除非该文件被保留为正式回归测试。
- 使用系统已安装的 Shell、Git、Node.js 和 Codex 自带工具
- 运行仓库中已有且无需安装依赖的测试

禁止：

- 修改 `D:/FiveRealms` 之外的文件
- 扫描或读取与任务无关的个人目录、附件、配置或凭据
- 修改 Codex、Git、Node.js、PowerShell 或系统配置
- 安装、更新或卸载依赖
- 执行工作区外的项目代码
- 使用脚本绕过工作目录限制
- 发起与当前任务无关的网络请求
- 创建或修改与当前 Bug 无关的文件

用户在当前任务中明确提供的附件可以只读查看，但不得扫描附件目录或读取其他历史附件。

如果任务必须修改工作区外文件、安装依赖、访问外部服务或更改系统配置，应立即停止并说明原因。

## 三、Git 限制

工作分支固定为 `deepseek-fixes`。 - 开始任务时必须确认当前分支为 `deepseek-fixes`。 - 不得要求或自行切换到 `main`。 - 如需确认与 `main` 是否同步，只允许进行只读比较。 - 如果当前不在 `deepseek-fixes`，应停止并报告，不得执行 `git switch` 或 `git checkout`。

开始工作前必须执行只读 Git 检查，例如：

```
git branch --show-current
git status --short
git log -1 --oneline
```

允许的 Git 操作包括：

```
git status
git status --short
git branch --show-current
git diff
git diff --check
git diff --stat
git log
git show
```

禁止所有 Git 写操作，包括但不限于：

```
git add
git commit
git push
git pull
git merge
git rebase
git reset
git checkout
git switch
git clean
git stash
git tag
```

Git 提交、推送和合并全部由用户手动完成。

如果工作区已有未提交修改：

- 必须保留用户已有内容。
- 只修改当前 Bug 所必需的部分。
- 不得覆盖、撤销或格式化用户已有修改。
- 如果无法安全区分用户修改和本次任务修改，应停止并报告。

## 四、根因定位要求

修改前必须搜索并阅读当前 Bug 的直接相关代码和测试，确认：

1. Bug 的实际触发路径
2. 直接根因
3. 正确规则
4. 合法性判断发生在哪里
5. 实际效果结算发生在哪里
6. 玩家和 AI 是否存在同类逻辑
7. 是否存在二次校验或重复校验
8. 是否存在取消、拒绝、无合法目标、角色死亡或状态变化等分支

不要仅修复表面 UI，而忽略底层游戏状态。

不要仅修改入口判断，而忽略结算阶段的再次判断。

不要仅修改玩家逻辑，而忽略 AI 使用的对应条件。

不要为了“保险”进行全项目审查。检查范围应限制在当前 Bug 的直接调用链和同源分支内。

## 五、正确规则的判断顺序

正确行为按以下优先级判断：

1. 用户当前任务中明确说明的规则
2. 用户指定的规则文档
3. 当前代码中多个直接相关模块一致采用的行为
4. 与当前 Bug 直接相关的现有测试

现有测试不一定代表最终正确规则。

如果用户明确规则与测试冲突：

- 以用户明确规则为准。
- 只修改与当前 Bug 直接相关的测试。
- 在最终报告中说明原测试与正确规则的冲突。

如果阅读代码和规则后仍无法判断正确行为，应停止修改，并说明缺少的关键信息。

## 六、修改范围

允许修改：

- 用户明确指定的文件
- 经实际代码定位后确认与当前 Bug 直接相关的其他文件
- 当前 Bug 的测试文件
- 浏览器构建版本同步涉及的静态资源引用文件

发现一个新的直接相关文件时，不需要立即停工申请，但必须确认其与根因或调用链存在直接关系，并在报告中列出修改原因。

禁止：

- 无关重构
- 大范围格式化
- 无关变量、函数或类重命名
- 公共接口调整
- 目录整理
- 清理旧代码
- UI 风格调整
- 用户本次任务未明确要求的游戏规则或平衡修改
- 依赖升级
- 无关测试修改
- 为未来功能提前设计接口
- 创建仓库中不存在且当前修复不需要的新抽象

发现其他 Bug 时，只记录，不得顺手修复。

## 七、JavaScript 检查重点

根据当前 Bug 的调用链，重点检查：

- 是否遗漏 `await`
- 是否将 Promise 当作布尔值
- 是否重复调用、重复绑定或重复结算
- 是否在遍历过程中错误修改数组
- 是否混淆对象引用、浅复制和独立状态
- 是否使用过期状态、旧目标或旧选择结果
- 是否遗漏 `null`、`undefined`、空数组或非法数值
- 玩家和 AI 是否采用不同的合法性条件
- 决策阶段和结算阶段是否采用不同规则
- 目标选择后是否存在不一致的二次验证
- 状态变化或角色死亡后是否继续使用旧对象
- 被调用的函数、字段、事件和 API 是否真实存在

不得凭空创造仓库中不存在的函数、字段或接口。

## 八、浏览器构建版本

FiveRealms 使用 `?build=` 参数使浏览器缓存失效。

只要修改了浏览器会实际加载的以下资源之一：

- HTML
- CSS
- JavaScript
- ES Module

就必须更新统一构建标识。

修改前先搜索当前构建标识及引用位置。

要求：

- 沿用项目当前的构建标识命名格式。
- 整个浏览器模块图使用同一个新标识。
- `index.html`、CSS 引用和所有静态模块导入保持一致。
- 只替换完整的 `?build=旧标识`。
- 构建版本同步文件中不得夹带其他修改。
- 更新后必须运行仓库已有的统一构建版本测试。

如果搜索到少量不同标识：

- 先判断是否属于独立资源、历史文件、测试文件或正常例外。
- 不要因为存在差异立即停止。
- 只有在无法判断浏览器实际加载关系时才停止并报告。

只修改测试、文档或浏览器不会加载的文件时，不需要更新构建标识。

## 九、测试要求

测试按以下顺序执行：

1. 运行与当前 Bug 直接相关的测试。
2. 如果缺少覆盖，补充最小且直接相关的回归测试。
3. 如果仓库存在明确且无需安装依赖的完整测试入口，运行完整测试。
4. 如果修改了浏览器资源，运行统一构建版本测试。
5. 查看最终 diff。
6. 执行：

```
git diff --check
```

测试不得：

- 安装依赖
- 修改系统或运行时配置
- 使用来源不明的外部脚本
- 执行工作区外的项目代码
- 通过修改无关测试来制造通过结果

测试失败时，应判断：

- 是否由本次修改造成
- 是否能在当前 Bug 范围内最小修复
- 是否属于已有的无关失败
- 是否因环境缺失而无法运行

与本次修改直接相关的问题可以继续修正并重新测试。

无关失败、历史失败或环境问题应如实报告，不得顺手扩大修复范围。

不得将“部分测试通过”描述为“全部测试通过”。

不得猜测测试数量或结果。只报告测试工具实际显示的信息；没有明确总数时，不自行编造或推算。

### 测试组织规范（强制）

新增或修改测试时，必须遵循以下高层规则，详细规则统一参见仓库根目录 `test.md`：

1. 新增或修改测试时，必须按 `tests/run.mjs` 当前稳定分类体系放到正确区域，不得习惯性追加到文件末尾。
2. 测试按“主要被测对象”归类，判断该测试主要在证明哪个系统或行为；不得仅根据名称关键词、调用链中碰巧涉及的模块或历史新增批次决定位置。
3. AI 专属测试必须集中到 `AI 系统` 下的对应子组；真实 Game/RuleEngine 规则仍放真实功能区域。
4. 角色测试原则上只使用两个固定入口：`角色规则 → 角色名` 与 `AI 系统 → AI 角色行为 → 角色名`。
5. 测试名称遵循稳定格式：普通 `主题：行为`；AI `AI·主题：行为`；UI `UI·主题：行为`。
6. 禁止通过删除、弱化或修改无关测试制造通过。
7. 纯测试结构整理（移动、重命名、格式）不得改变测试语义。
8. 只修改测试 `.mjs`、`AGENTS.md`、`test.md` 等浏览器不会加载的文件时，不需要更新 `?build=`。
9. 浏览器实际加载的 HTML/CSS/JS/ES Module 修改仍必须执行统一 build 更新。
10. 详细测试结构、分类、命名、Helper 与结构整理规则统一参见 `test.md`，不要在 AGENTS.md 重复整份 `test.md` 内容。

### Balance 平衡测试

当用户要求检查平衡，或本次修改涉及规则、技能、卡牌数量、AI 权重等可能影响平衡的内容时，必须先阅读仓库根目录 `test.md`，并以其中当前记录的脚本入口、参数和判断方法为准，不得沿用旧命令或自行简化。

日常平衡验证默认使用：

```
npm run test:balance -- `
  --games 200 `
  --reserve-games 24 `
  --workers 24 `
  --progress `
  --progress-every 10 `
  --no-stdout `
  --summary `
  --game-timeout-ms 120000 `
  --retries 0 `
  --output reports\balance-200.json
```

只有在需要固定连续局号、严格复现或对比同一批种子时，才按 `test.md` 使用 `--reserve-games 0` 的严格模式。

要求：

- 正式结果写入 `reports` 目录，终端默认只保留进度和摘要，不输出完整 JSON。
- 不得把备用候选模式的“先完成样本”与严格固定样本混为一谈。
- 必须报告实际参数、报告路径、完成局数、停滞数、失败数及测试工具输出的关键摘要。
- UI、文档或纯展示修改若不会影响规则、AI 或平衡，不要求运行 Balance 测试。

## 十、执行流程

每次任务按以下流程完成：

1. 阅读用户描述的 Bug 现象和正确规则。
2. 查看当前分支和 Git 工作区状态。
3. 搜索直接相关的业务代码和测试。
4. 阅读调用链，确认根因。
5. 检查玩家、AI、合法性、结算和直接相关边界分支。
6. 完成最小合理修改。
7. 补充或更新直接相关测试。
8. 如涉及浏览器资源，统一更新 `?build=` 标识。
9. 运行相关测试。
10. 运行可用的完整测试。
11. 如修改了浏览器实际加载资源，运行统一构建版本测试。
12. 检查 `git diff`、`git diff --stat` 和 `git diff --check`。
13. 确认没有无关修改。
14. 输出简洁报告并停止。

只要修改仍明确属于当前 Bug 的直接范围，定位根因后不需要等待用户再次批准。

## 十一、必须停止的情况

仅在以下情况停止：

### 1. 无法确定正确规则

例如：

- 用户描述存在关键歧义
- 规则文档互相冲突
- 关键函数、字段或状态含义无法确认
- 修复必然改变另一项已确认规则

### 2. 必须越过任务权限

例如：

- 必须修改工作区外文件
- 必须安装依赖
- 必须修改系统或工具配置
- 必须访问外部服务
- 必须执行 Git 写操作

### 3. 无法在当前范围内安全修复

例如：

- 必须进行大范围重构或公共接口调整
- 用户已有修改无法安全保留
- 测试失败且无法在当前 Bug 范围内解决
- 浏览器实际行为与源码、测试严重矛盾，需要用户提供实际加载信息

不要因为以下情况轻易停止：

- 需要多修改一个直接相关文件
- 测试命令名称与预期略有不同
- 代码结构比预期复杂
- 存在轻微但可以通过继续阅读解决的不确定性
- 当前测试覆盖不足但可以补充直接相关测试

停止时必须说明：

1. 停止原因
2. 已确认的代码或测试证据
3. 尚未确认的内容
4. 需要用户决定或提供的信息
5. 推荐的下一步检查

## 十二、完成报告

最终报告保持简洁，使用以下结构。

### 根因

说明：

- Bug 的直接原因
- 相关调用链
- 结论来自代码、测试还是尚未完全验证的推测

### 修改文件

按用途列出：

- 业务代码
- 测试
- 构建版本同步

说明每个文件为什么与当前 Bug 直接相关。

### 修改内容

说明每个修改点解决了什么问题。

如果更新了构建标识，说明：

- 原标识
- 新标识
- 是否已在浏览器模块图中统一

### 验证结果

列出实际执行的命令及实际结果，包括：

- 相关测试
- 完整测试
- 构建版本测试
- `git diff --check`
- 如运行了 Balance 测试：实际模式、参数、报告路径、完成局数、停滞数、失败数和关键摘要

只报告实际执行和实际输出，不得猜测。

### 未验证内容

例如：

- 尚未进行浏览器人工验证
- 某随机场景无法稳定复现
- 某测试因环境原因未运行
- 完整测试入口不存在或无法确认

### 范围确认

简要说明：

- 是否存在无关修改
- 是否安装依赖
- 是否执行 Git 写操作
- 是否修改工作区外文件
- 是否发现但未处理其他问题

完成报告后停止工作，等待用户进行浏览器人工验证以及 Git 提交、推送和合并。