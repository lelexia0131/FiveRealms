# FiveRealms Agent Instructions

## 1. 基本工作原则

本项目当前主要进行明确、局部、可验证的 Bug 修复。

每次任务只允许处理用户本次明确指定的一个 Bug。

不得将多个问题合并处理。发现其他问题时，只能在最终报告中说明，不得顺手修改。

在修改代码前，必须先定位问题的直接原因，并确认相关调用链、状态变化和现有规则。

不得仅根据函数名、注释或猜测修改代码。必须以当前仓库中的实际实现为准。

如果信息不足以确认正确行为，必须停止修改并向用户说明缺少的信息，不得自行设计规则。

------

## 2. 工作区与文件权限边界

只允许在 Codex App 当前打开的 FiveRealms 项目工作文件夹内读取、搜索、测试、创建和修改文件。

禁止访问、创建、删除、移动或修改当前工作文件夹之外的任何文件。

禁止访问或修改以下位置：

- 用户主目录中的其他文件
- `.codex` 配置目录
- 桌面和下载目录
- Windows 系统目录
- 其他项目目录
- 其他磁盘中的文件
- 工作文件夹之外的 Git 仓库

不得通过以下方式绕过工作文件夹边界：

- 使用工作区之外的绝对路径
- 使用 `..` 跳转到父目录
- 创建或跟随指向工作区外部的符号链接
- 调用脚本间接修改工作区外部文件
- 修改 Codex、PowerShell、Git 或系统级配置

如果任务确实需要访问或修改工作文件夹之外的文件，必须立即停止，并向用户说明：

1. 需要访问的准确路径
2. 需要进行的操作
3. 为什么本任务必须进行该操作
4. 是否存在项目内替代方案
5. 可能产生的风险

未经用户明确批准，不得继续该操作。

------

## 3. 单次任务修改范围

每次任务提示词中列出的“允许修改文件”构成本次任务的业务文件白名单。

只有白名单中的业务文件允许进行逻辑、规则、测试、UI 或样式修改。

其他项目文件默认只允许读取，不允许修改。

如果没有明确给出文件白名单，应先完成只读定位，说明预计需要修改的文件，然后等待用户确认，不得直接修改多个文件。

如果在修复过程中发现必须修改白名单之外的业务文件，必须停止修改并报告：

- 文件路径
- 必须修改的原因
- 预计修改内容
- 不修改该文件可能造成的结果

未经用户明确批准，不得自行扩大业务文件白名单。

### 构建版本同步例外

如果本次任务修改了浏览器实际加载的 HTML、CSS 或 JavaScript 文件，则必须同时执行第 7 节规定的统一构建版本同步。

统一构建版本同步属于修复完成所必需的缓存失效操作，不视为无关业务修改。

进行统一构建版本同步时，自动追加一个范围严格受限的辅助白名单：

- `index.html`
- 仓库内所有包含当前 `?build=` 模块导入参数的 JavaScript 文件

在这些辅助白名单文件中，只允许把完整旧构建标识精确替换为同一个新构建标识。

不得借构建版本同步修改这些文件中的任何其他代码、注释、格式或导入路径。

如果无法通过精确替换完成统一版本同步，必须停止并报告，不得自行扩大修改范围。

------

## 4. 禁止无关修改

不得进行与当前 Bug 无直接关系的任何修改，包括但不限于：

- 重构代码
- 整理目录
- 批量格式化
- 调整缩进或换行
- 改变量名、函数名或类名
- 更改公共接口
- 提取公共函数
- 新增抽象层
- 更新注释风格
- 清理所谓的旧代码
- 删除看似未使用的代码
- 修改游戏平衡参数
- 修改卡牌、角色或技能规则
- 修改 UI 文案或样式
- 升级依赖
- 安装新依赖
- 修改构建工具
- 修改 Codex 配置
- 修改与当前 Bug 无关的测试

不得以“提高可读性”“顺便优化”“统一风格”或“避免未来问题”为理由扩大改动范围。

修复应优先采用能够解决根因的最小改动。

第 7 节规定的统一构建版本替换是浏览器代码修改后的必要发布步骤，不属于无关格式化或无关批量修改。

但版本同步只能修改完整的 `?build=` 标识，不得顺便修改其他内容。

------

## 5. 游戏规则与现有行为

不得自行推测或重新设计 FiveRealms 的规则。

正确行为的优先级为：

1. 用户当前任务中明确说明的规则
2. 用户指定的项目规则文档
3. 当前代码中多个模块一致采用的实际规则
4. 现有测试所表达的行为

如果这些来源相互冲突，必须停止并报告冲突，不得自行选择一个版本。

修复某个逻辑分支时，必须检查是否同时存在：

- 玩家分支与 AI 分支
- 主动使用与被动触发分支
- 卡牌合法性检查与实际结算
- 目标选择与目标再次验证
- 同步与异步调用
- 取消、拒绝、超时或无合法目标流程
- 玩家死亡、离场或状态变化后的后续流程
- UI 显示状态与底层游戏状态

只检查与当前 Bug 直接相关的分支，不得借此审查或改写整个系统。

------

## 6. JavaScript 特别检查项

修改 JavaScript 逻辑时，应重点检查：

- 是否遗漏 `await`
- 是否把 Promise 当作布尔值使用
- 是否重复绑定事件监听器
- 是否导致同一效果重复结算
- 是否在遍历过程中错误修改原数组
- 是否混淆对象引用与对象副本
- 是否修改了过期状态快照
- 是否遗漏 `null` 或 `undefined`
- 是否在目标状态变化后继续使用旧目标
- 是否只修改 UI 而没有修复底层状态
- 是否只修复玩家流程而遗漏 AI 流程
- 是否绕过已有的合法性校验入口
- 是否创造仓库中不存在的函数、字段或 API

不得因为某个函数或属性“看起来应该存在”就直接调用。使用前必须在仓库中确认其真实定义及调用方式。

修改浏览器加载的 JavaScript 文件后，还必须执行第 7 节规定的统一构建版本同步。

------

## 7. 浏览器缓存与统一构建版本

FiveRealms 使用 URL 中的 `?build=` 查询参数使浏览器和静态服务器缓存失效。

本项目将构建标识视为整个浏览器模块图的统一版本号，而不是单个文件各自独立的缓存编号。

### 7.1 必须更新版本的情况

本次任务只要修改了以下任一类浏览器实际加载的文件，就必须生成一个新的统一构建标识：

- `index.html`
- 任意浏览器加载的 JavaScript 文件
- 任意浏览器加载的 CSS 文件
- 会通过 ES Module 导入进入浏览器模块图的其他文件

只修改以下内容时，通常不需要更新浏览器构建版本：

- `README.md`
- `AGENTS.md`
- 仅供 Node 测试使用且浏览器不会加载的测试文件
- 不参与浏览器运行的文档或开发说明

如果无法确认某个文件是否进入浏览器模块图，应先只读检查其实际加载关系，不得猜测。

### 7.2 全项目必须使用同一个构建标识

更新构建版本时，必须保证以下位置全部使用完全相同的新标识：

1. `index.html` 中所有 CSS 的 `?build=` 参数
2. `index.html` 中主入口 JavaScript 的 `?build=` 参数
3. 所有 JavaScript 文件中的静态 ES Module 导入参数
4. 所有 JavaScript 文件中的副作用导入参数
5. 仓库中其他受现有统一版本测试约束的浏览器资源引用

不得出现以下情况：

- 入口使用新版本，CSS 仍使用旧版本
- `main.js` 使用新版本，其他模块仍使用旧版本
- 只更新直接导入链
- 不同模块分别使用不同的版本号
- 同一次任务产生多个不同的新构建标识
- 只依赖用户执行 `Ctrl+F5`，但不更新统一版本号

即使本次只修改了一个 JavaScript 文件，也必须同步更新整个浏览器模块图中的构建标识。

### 7.3 替换方式

开始替换前，应只读搜索仓库中现有的所有构建标识。

必须确认当前浏览器模块图使用的旧标识。

如果修改前已经存在多个不同的构建标识，必须停止并报告，不得猜测应以哪一个为准。

确认旧标识后，只允许进行完整字符串的精确替换，例如：

```text
?build=旧构建标识
```

替换为：

```text
?build=新构建标识
```

不得：

- 使用模糊替换修改其他字符串
- 修改导入路径
- 修改模块名称
- 修改业务代码
- 修改注释
- 格式化整个文件
- 批量替换与 `?build=` 无关的内容

### 7.4 新构建标识

新标识应：

- 与旧标识不同
- 在本次任务的所有位置完全一致
- 沿用项目现有命名格式
- 能够明确区分于此前已经发布或测试过的版本

如果用户在当前任务中指定了新标识，必须使用用户指定值。

如果用户没有指定，可以根据项目现有格式生成一个简短、稳定的新标识，但必须在修改报告中明确说明最终使用的值。

不得为不同文件分别生成版本号。

### 7.5 测试要求

统一构建版本更新完成后，必须运行仓库现有测试。

必须特别确认以下测试通过：

```text
浏览器模块图使用统一构建版本，静态服务器不会复用旧规则模块
```

该测试失败时，不得忽略、删除、放宽或绕过断言。

不得仅以“浏览器技术上允许不同版本号”为理由忽略该测试，因为本项目已经明确采用全模块统一版本策略。

只有统一构建版本测试及本次相关测试全部通过，才能报告任务完成。

### 7.6 浏览器验证

自动化测试通过后，如果本次 Bug 涉及浏览器实际交互，还应提示用户：

1. 重新启动或确认静态服务器目录正确
2. 重新加载页面
3. 必要时执行 `Ctrl+F5`
4. 按原始复现步骤进行人工验证

不得声称已经完成用户浏览器中的人工验证，除非实际进行了该验证。

------

## 8. Git 与版本控制

默认只允许执行只读 Git 命令，例如：

```text
git status
git diff
git diff --stat
git log
git show
```

禁止执行以下操作，除非用户在当前任务中明确授权：

```text
git add
git commit
git push
git pull
git merge
git rebase
git reset
git checkout
git switch
git clean
git stash
git tag
```

不得自行创建分支、提交代码、推送远程仓库或清理用户的未提交修改。

必须保留任务开始前已经存在的用户修改。

不得覆盖、回滚或格式化用户已有的未提交代码。

如果目标文件在任务开始前已经存在未提交修改，必须先报告，并在修改时只触碰本任务必需的行。

Git 暂存、提交、推送和分支合并默认由用户手动完成。

完成修改、测试和 diff 检查后应立即停止，不得继续生成或执行提交操作。

------

## 9. 命令与依赖限制

不得安装任何依赖、工具、Skill、插件或 MCP 服务。

不得执行：

```text
npm install
pnpm install
yarn install
pip install
winget install
choco install
```

除非用户在当前任务中明确授权。

不得启动与当前 Bug 无关的长期运行服务。

不得执行来源不明的脚本。

不得使用具有破坏性的命令，例如递归删除、强制覆盖或清理整个目录。

执行测试前，应优先使用仓库中已经存在且与当前修改直接相关的测试命令。

如果统一构建版本发生变化，必须运行能够检查整个浏览器模块图版本一致性的现有测试。

------

## 10. 标准处理流程

每个 Bug 按以下顺序处理：

1. 阅读用户提供的 Bug 现象和正确规则。
2. 查看 Git 状态，确认是否存在用户未提交修改。
3. 只读搜索与该 Bug 直接相关的代码。
4. 确认真实调用链和状态变化。
5. 给出简短的根因判断。
6. 确认业务修改文件处于当前任务白名单内。
7. 进行最小范围业务修改。
8. 判断本次是否修改了浏览器实际加载的 HTML、CSS 或 JavaScript。
9. 如果修改了浏览器资源，按照第 7 节统一更新所有 `?build=` 构建标识。
10. 执行与该 Bug 直接相关的测试。
11. 执行统一构建版本测试。
12. 查看最终 diff。
13. 检查是否存在无关修改。
14. 向用户报告结果。
15. 停止并等待用户手动处理 Git 提交或后续操作。

如果根因尚未确认，不得进入第 7 步。

如果浏览器资源已经修改但统一构建版本尚未更新，不得报告任务完成。

如果测试未全部通过，不得建议合并到主分支。

------

## 11. 修改完成后的报告

完成任务后必须说明：

### 根因

说明导致 Bug 的直接原因，不得只复述现象。

### 修改文件

列出所有被修改文件的相对路径。

必须将文件区分为：

- 业务逻辑或测试修改
- 统一构建版本同步修改

### 修改内容

逐项说明每个修改点及其必要性。

如果进行了构建版本同步，必须说明：

- 原构建标识
- 新构建标识
- 替换范围
- 是否所有浏览器模块和样式表已经统一
- 是否只修改了 `?build=` 参数

### 验证结果

列出实际执行的测试或人工验证步骤。

不得声称执行了没有真正运行的测试。

必须明确报告：

- 通过测试数量
- 失败测试数量
- 统一构建版本测试是否通过

只要存在失败测试，就不得写“全部验证通过”。

### 未验证内容

明确说明由于环境、依赖或上下文限制而未能验证的部分。

### 范围检查

明确确认：

- 是否只修改了业务文件白名单中的业务内容
- 是否只在辅助白名单中修改了构建标识
- 是否存在无关格式变化
- 是否访问或修改了工作文件夹外的文件
- 是否安装了依赖
- 是否执行了 Git 写操作
- 是否发现但未处理其他问题

------

## 12. 停止条件

遇到以下情况必须停止，不得猜测：

- 无法确定正确游戏规则
- 用户描述与代码实现明显冲突
- 必须修改业务文件白名单之外的文件
- 统一构建版本无法通过精确替换完成
- 修改前已经存在多个无法解释的构建标识
- 必须访问工作文件夹之外的路径
- 必须安装新依赖
- 必须改变公共接口
- 必须进行大范围重构
- 目标文件存在难以区分的用户未提交修改
- 无法确认某个字段、函数或事件是否真实存在
- 修复一个 Bug 必然会改变其他既有规则
- 统一构建版本测试失败且无法在授权范围内修正
- 本次相关测试或完整测试仍然失败

停止时只报告原因、证据和下一步建议，不得自行扩大任务范围。